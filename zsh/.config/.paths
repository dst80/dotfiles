#!/bin/sh

pathadd() {
  for ARG in "$@"
  do
    if [ -d "$ARG" ] && [[ ":$PATH:" != *":$ARG:"* ]]; then
        PATH="${PATH:+"$PATH:"}$ARG"
    fi
  done
}

pathadd ~/.local/bin ~/bin ~/go/bin ~/.cargo/bin
